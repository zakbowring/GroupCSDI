#pragma once
class Project
{
public:
	//Here we need the function declarations for the following functions:
	//Add a project
	//Validate a project (this function will check to see if it exists)
	//Update a project
	//Delete a project
	Project();
	~Project();
};

